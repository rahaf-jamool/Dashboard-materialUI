export const SET_TOKEN = (state, token) => {
  state.token = token;
};

export const SET_USER = (state, data) => {
  state.user = data;
};

//register
export const SET_TOKEN1 = (state, token) => {
  state.token = token;
};